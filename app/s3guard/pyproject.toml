[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "s3guard"
version = "0.1.0"
description = "S3 security auditor agent with approval-gated remediation, on Bedrock AgentCore Runtime"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aws-opentelemetry-distro",
    "bedrock-agentcore >= 1.9.1",
    "botocore[crt] >= 1.35.0",
    "strands-agents >= 1.15.0",
    
]

[tool.hatch.build.targets.wheel]
packages = ["."]
